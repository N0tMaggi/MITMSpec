[project]
name = "mitmspec-proxy-addon"
version = "0.1.0"
description = "MITMSpec mitmproxy addon for normalized traffic export"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
