{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "PlatformMetadata": {
    "PlatformName": "MITMSpec",
    "ControlPlane": "ASP.NET Core 10 modular monolith",
    "GatewayAgent": "Go host agent",
    "ProxyIntegration": "Python mitmproxy addon",
    "PrimaryStorage": "PostgreSQL 16+",
    "WindowsPackaging": "WiX v4 MSI + EXE bootstrapper",
    "TopPriorities": [
      "Freeze TrafficEnvelope v1 and quarantine behavior",
      "Prove the Linux lab path end to end",
      "Implement identity, provisioning, and CA lifecycle",
      "Ship the Linux gateway MVP before Windows packaging"
    ]
  }
}
