# Windows Deployment Assets

This directory is reserved for Windows-specific deployment assets such as:

- WiX installer sources
- service installation templates
- bootstrapper configuration
- parity validation helpers
